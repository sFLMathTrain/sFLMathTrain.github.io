//- jQuery
script(src="http://code.jquery.com/jquery.min.js")
//- RequireJS
script(src="https://cdnjs.cloudflare.com/ajax/libs/require.js/2.1.16/require.min.js")
//- Bootstrap [rel]
script(src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js")
link(rel="stylesheet", href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css")
//- Your scripts
script(type="application/javascript", src="./min_href.js")
//- Bootstrap [> BODY <] sizer
script(type="application/javascript").
    setTimeout(function() {
        if (
            (
                document.body.style.marginTop === "" ||
                document.body.style.marginTop === "0px"
            ) &&
            (
                document.body.style.marginBottom === "" ||
                document.body.style.marginBottom === "0px"
            )
        ) {
            document.body.style.transition = (1/4)+"s ease-in-out";
            document.body.style.marginBottom = "3%";
            document.body.style.marginTop = "3%";
        }
    }, 1 * 1000);
script(type="application/javascript").
    // NavyCommanders COL_Formatter!
    $.fn.col_format = function() {
        var s;
        var children = $(this).children();
        var cl = children.length;
        if ($(this).hasClass("row") === true) {
            for (var i = 0; i < cl; i++) {
                // Set Size
                if (cl === 1) {
                    s = 12;
                } else if (cl === 2) {
                    s = 6;
                } else if (cl === 3) {
                    s = 4;
                } else if (cl === 4) {
                    s = 3;
                } else if (cl > 4) {
                    s = 12;
                }
                // Do shit
                if ($(children[i]).hasClass("col") === true) {
                    $(children[i]).toggleClass("col-md-" + s);
                    $(children[i]).toggleClass("col");
                }
            }
        }
        return (cl) + " | " + s;
    };
    setTimeout(function() {
            // Example:
    // vvvvvvvvvvvvvvvvvvvvvvvvvvvvvvvvvvvv
        $(".row:has(.col)").each(function() {
            $(this).col_format();
        });
    }, 0.1 * 1000)